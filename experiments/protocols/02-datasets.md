# Protocol 02 — Datasets (HUMAN)

Document each dataset in `experiments/datasets/README.md` with: URL, license, digests, local path (gitignored), and whether ground-truth neighbors are included or must be computed.

Do not commit multi-GB binaries to git without LFS decision from Vijay.
