# Protocol 00 — Overview

1. Freeze LogicalPlan JSON schema before claiming optimiser results.
2. For each config row: record plan hash, backend versions, seeds.
3. Separate columns: AUTHOR_CLAIM vs REPRODUCED.
4. ACL tests must attempt illicit access and expect denial.
5. Never paste numbers from papers into `results/` as if local.
